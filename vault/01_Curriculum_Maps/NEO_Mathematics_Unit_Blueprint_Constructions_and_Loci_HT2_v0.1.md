# NEO Mathematics — Unit Blueprint: Constructions, Loci and Bearings (Exact Geometry with Two Tools)
**Pathway:** Foundations (internal stage code Y9) · **Term:** Autumn, Half-Term 2 — second unit (follows Quadratics)
**Shape:** 7 lessons · 45 minutes each · fills roughly the second half of the 14-lesson Year 9 half-term. *(May flex to 8 if bearings splits into an intro and a secure lesson — decided live from evidence, per the responsive envelope.)*
**Qualification overlay:** NC KS3–KS4 Geometry — ruler-and-compass **constructions** (equilateral triangle, perpendicular bisector, angle bisector, perpendicular to/from a line), **loci** and regions, and **bearings** — building towards Pearson Edexcel International GCSE (4MA1) Geometry (Constructions, loci and bearings). **Out of scope for this introductory unit:** constructing a 30°/45° angle as named exam tasks (touched only as extensions of bisection), formal congruence proofs, and circle theorems.
**Strand:** Geometry — the pathway's **first constructions unit**; draws on the 2D & 3D Shapes unit (triangle and quadrilateral properties), the Introduction to Trigonometry unit (measuring and drawing angles), and Ratio (scale, for bearings-and-distance).
**Status (v0.1):** blueprint framed; the reusable Dynamic Geometry Studio and the lessons to be built and reviewed one at a time.

## Unit Driving Question
With just a straightedge and a pair of compasses, how can we build shapes that are *exactly* right — and find *every* point that satisfies a rule?

## The mathematical story
Most drawing is approximate; **construction** is exact. This unit is about the surprising power of just two tools — a straightedge (an unmarked ruler, for straight lines) and a pair of **compasses** — to produce geometry you can *trust*, and about the single idea that makes them work.

That idea is **equal distance**. A circle is not really "a round shape"; it is **the set of all points the same distance from a centre**. The compass is a machine for "same distance", and once a learner sees that, every construction in the unit falls out of it. Copy a length by opening the compass to it. Build a triangle from three sides (SSS) as the meeting point of two circles. Find the **perpendicular bisector** — and see that it is not just a construction but a **locus**: the set of all points *equidistant from two fixed points*. Find the **angle bisector**, and see that it too is a locus: the set of all points *equidistant from two lines*. So the constructions and the loci are the same idea wearing two hats — *distance conditions drawn exactly*.

From there the unit widens. A **locus** is the path or region of every point obeying a condition: a fixed distance from a point is a **circle**; a fixed distance from a line is a **pair of parallel lines**; equidistant from two points is a **perpendicular bisector**; equidistant from two lines is an **angle bisector**. **Bearings** add *direction* to *distance* — a three-figure angle measured clockwise from north — so a position becomes "so far, in that direction", and a scale turns the drawing back into real ground. The unit closes with **compound constructions in context**: real "where should it go?" problems whose answer is the point or region where several loci meet — siting something on a scaled plan so that it satisfies two or three conditions at once.

**Arc:** the compass *is* equal distance → construct triangles and quadrilaterals from that → the perpendicular bisector as the *equidistant-from-two-points* locus → the angle bisector as the *equidistant-from-two-lines* locus → loci and regions in general → bearings (direction + distance + scale) → compound constructions that place a point where conditions meet.

This is the pathway's first sustained work in **exact geometry**, so it deliberately builds the habit that a construction must be *justified* by the equal-distance rule that produced it — the arcs are the argument, not decoration — the same reasoning-first stance the algebra units took towards structure.

## Stepping Stones
Understand a circle as the set of points a fixed distance from a centre, and use compasses to copy a length · construct a triangle given three sides (SSS), and given two sides and the included angle / two angles and a side using a protractor · construct simple quadrilaterals from given measurements · construct the **perpendicular bisector** of a segment, and describe it as the locus of points **equidistant from two points** · construct the **angle bisector**, and describe it as the locus of points **equidistant from two lines** · construct the perpendicular to a line **from** a point and **at** a point on the line · draw and interpret a **locus** for a given condition (fixed distance from a point, from a line, equidistant from two points or two lines) and shade the **region** satisfying an inequality condition · measure and draw **three-figure bearings**, and combine a bearing with a distance using a **scale** · solve a **compound construction** problem in context by finding where two or more loci meet on a scaled plan.

## Session sequence (7 × 45-minute lessons)
A **mid-unit checkpoint** sits after Lesson 4 — the point where the unit turns from *constructing exact objects* to *reasoning about sets of points (loci)*. Confirm that the compass-as-equal-distance idea, the two bisector constructions, and the equidistance interpretation are secure before moving into loci, bearings and compound problems.

| # | ID | Type | Lesson | Driving Question | Primary Cornerstone | Interactive (Studio preset) | Reconnection |
|---|----|------|--------|------------------|---------------------|-----------------------------|--------------|
| 1 | GC-01 | Core | The compass and the circle | How can one tool guarantee "exactly the same distance"? | Connection | Studio: compass + straightedge; copy a length; SSS triangle | ruler use · triangle properties |
| 2 | GC-02 | Core (tiered) | Constructing triangles and quadrilaterals | Can I build a shape that is exactly right from given measurements? | Movement | Studio: compass + protractor (SSS · SAS · ASA · quadrilaterals) | GC-01 · measuring angles |
| 3 | GC-03 | Core | The perpendicular bisector | Where are all the points the same distance from two points? | Reflection | Studio: perpendicular-bisector construction + locus trace | GC-01 · midpoint · equal radius |
| 4 | GC-04 | Core | The angle bisector | Where are all the points the same distance from two lines? | Reflection | Studio: angle-bisector + perpendicular-to-a-line construction | GC-03 · angles · perpendicular |
| — | — | — | **★ Mid-unit checkpoint** — are the compass idea, both bisectors, and the equidistance meaning secure? | — | — | — |
| 5 | GC-05 | Core | Loci and regions | What shape is "every point that obeys this rule"? | Connection | Studio: locus lab (distance from point/line; equidistant; shade regions) | GC-03/04 · circle · parallel lines |
| 6 | GC-06 | Core | Bearings | How do we say exactly where something is — direction and distance? | Movement | Studio: protractor/north + scale (measure & draw bearings) | angle measure · scale / ratio |
| 7 | GC-07 | Core | Compound constructions in context | Where must it go to satisfy every condition at once? | Creativity / Reflection | Studio: scaled plan; intersect several loci | GC-01–GC-06 |

**Bearings note (GC-06):** the lesson runs "intro → secure" within one 45-minute shape (three-figure notation and measuring first; drawing a bearing-and-distance to scale second). If evidence shows learners need more time, split into GC-06a (read/measure) and GC-06b (draw to scale), taking the unit to 8 lessons.

## Cornerstone emphasis across the unit
The natural spine is **Movement, Reflection and Connection**, with **Creativity** anchoring the applied finale. **Movement** is central and literal — swinging the compass, drawing along the straightedge, dragging a base point to watch a whole construction and its arcs update, and measuring bearings — this is where "construction" stops being a recipe and becomes an action the learner owns. **Reflection** carries the unit's deep idea: *why* a construction works — that the perpendicular bisector and angle bisector are **loci defined by equal distance**, and that the arcs are the justification. **Connection** binds constructions to loci to bearings: the same equal-distance idea generates the circle, both bisectors, and the compound regions. **Creativity** anchors Lesson 7, where learners pose and solve their own "where should it go?" problems. **Rest** anchors the mid-unit checkpoint and the final consolidation.

### Nutrition — predominantly Mode B (intellectual), with an honest Mode A finale
Per GreenPrint D6, Mode A (contextual) is reserved for mathematics that genuinely lives in food, hydration, body, health, sustainability or budgeting. Most of this unit does not, so it must **not** let "real-world" plans masquerade as the Nutrition Cornerstone. Across GC-01–GC-06 the honest mode is **Mode B — intellectual**, naming what the *learning* feeds: the deep satisfaction of **certainty from constraint** (that two humble tools, used honestly, produce results you can *prove*, not merely eyeball); the elegance of one idea — equal distance — generating a whole family of constructions; and the power of a **locus** as a way of thinking about *all* the solutions at once rather than one at a time.

**Lesson 7 may use Mode A honestly** where a compound-construction context genuinely lives in those domains — for example siting a **water trough or a new planting bed** equidistant from two paths and within reach of a tap (food-growing / sustainability / hydration), or placing a **well** to serve two dwellings fairly. Where the context is a treasure map or a phone-mast coverage problem, it is used honestly as a **problem setting** and motivation, but the Nutrition panel stays **Mode B**.

## Reconnection Routes used
Measuring a length accurately with a ruler (KS3) · measuring and drawing angles with a protractor (from the Introduction to Trigonometry unit) · properties of triangles and quadrilaterals — side and angle facts (from the 2D & 3D Shapes unit) · the idea of a circle and its radius (new to the register, introduced in GC-01) · parallel and perpendicular lines (KS3 / Straight Lines) · scale and ratio, and converting between a scaled drawing and real distances (from Ratio, for bearings). Several **construction-facing routes are new to the Foundations register** (the compass as an equal-distance tool, locus language, three-figure bearings) and will need a Stage 2 option bank before build, per the coverage-map convention for unbuilt routes.

## Resource reuse — audit before building
- **Dynamic Geometry Studio (GC-01–GC-07):** the unit's spine, and a **new reusable environment** — audited against existing interactives (the Dynamic Graph Lab / Parabola Lab is coordinate-and-curve based; the triangle engines from Trigonometry draw fixed labelled triangles; neither supports free construction), so a purpose-built dynamic-geometry engine is warranted. **Agreed scope (curriculum lead, this session):** a *fuller Sketchpad-style* studio with **authentic instruments that leave visible arcs**. Core capabilities: draggable points on a live **dependency graph** (drag a base point and the whole construction, including arcs, updates); a **compass** set to a radius and swung, and a **straightedge/segment** drawn along, both leaving their marks; **construct operations** (intersection, midpoint, perpendicular bisector, angle bisector, perpendicular/parallel line, locus/trace); **measurements** (length, angle, and a calculator read-out); **transformations** (reflect, rotate, translate, enlarge); a **protractor + north/bearings** mode with a scale; **animation** of a driver point; and **custom (saved) tools** so a completed construction can be reused. Fully inline, no third-party or CDN calls; honours *Reduce motion*; keyboard-reachable; within the visual-containment gate. **Build once as a standalone engine, sign off the architecture, then preset per lesson** (compass-and-straightedge → protractor triangles → bisectors → locus lab → bearings → scaled-plan compound). This mirrors how the Dynamic Graph Lab was built once and preset across Straight Lines and Quadratics.
- **Protractor / angle tools:** reuse and generalise the angle-measurement and drawing components from the Introduction to Trigonometry unit inside the Studio rather than writing new ones where possible.
- **Scratchpad:** the constructions Scratchpad background should offer a plain and a light-grid page and be genuinely optional; the Studio, not the Scratchpad, is where exact construction happens.

## Build approach
Because the Studio is a large, shared component, it is built and signed off **as a standalone reusable engine first** (architecture spec → build → QA of the engine itself), before GC-01. Thereafter, frame one lesson → present a 3–5 option interactive bank (Stage 2) drawn from Studio presets → build the full ecosystem (interactive, Practice Companion, Scratchpad, Learner + Supporting Adult guidance) → QA against the shipping gates **including QA gate 6a (equivalent-form answer acceptance)** → review. One lesson at a time; never pre-build ahead of review. British English, "learner" not "student", the six NEO Cornerstones in locked order, no chronological year labels in learner-facing text, timings as invitations, precise mathematical vocabulary (**perpendicular bisector, angle bisector, locus/loci, equidistant, bearing, construct** — glossed once on first use), honest Nutrition modes, and — for a constructions unit especially — the standard that a construction is judged by its **arcs and method**, not only its final picture. After each accepted lesson, offer to update the live Foundations Coverage Map (Stepping Stones, Reconnection Routes, status, evidence, header audit).

## Build note — GC-01 "The compass and the circle" (v0.1, built)
**Status:** live on the site (`lessons/constructions/01-the-compass-and-the-circle.html`); Learner + Supporting Adult guides generated; curriculum unit "Constructions, Loci and Bearings" added after Quadratics; Geometry Studio auto-published alongside.

**Hero interactive (Gerry's choice):** *Guided compass construction*. The four Cornerstone interactives are hand-built SVG (self-contained, no CDN), sharing a small construction engine (`circInt` two-circle intersection, `arcPts` sampled arcs, pointer→SVG mapping):
- **Connection — equidistant circle reveal:** point P is leashed to a fixed radius from centre O; dragging it traces the "same-distance" locus, and *Reveal* completes the circle. Teaches the circle as a locus before any construction.
- **Movement (hero) — guided equilateral:** step-gated buttons (arc from A · arc from B · mark crossing C · join), both arcs radius AB, live-recomputed as A/B are dragged, arcs left visible. QA asserts the built figure is provably equilateral (AB=AC=BC).
- **Reflection — why exactly equilateral:** click each side; the readout ties the equal length back to the shared radius AB.
- **Creativity (stretch) — SSS from three sides:** chip-chosen lengths; *Construct* swings two arcs of the chosen radii; the triangle inequality is discovered when the arcs fail to meet (e.g. 2, 3, 6).

**Standards met:** self-contained HTML; Practice Companion with the four-tier support ladder and per-question worked-solution gate; NEO Scratchpad (construction prompts, own storage key); focused palette [A, B, C, =, cm, °]; **QA gate 6a** (fraction-aware `num()`, fraction/½ acceptance) confirmed by jsdom; six Cornerstones in locked order; British English, "learner", no year labels in learner text; arcs-as-evidence standard honoured throughout.

**Guides engine:** added `constr_diagram(a,b,c)` to `_guides_template.py` (base AB, arcs of the two radii, apex C; dashed non-meeting arcs when the inequality fails) and a `dk=="construction"` branch in `_wex_card`. Worked examples: (1) equilateral on AB = 6 cm; (2) SSS 6-5-7.

## Studio-launch pattern (v0.4) — learners construct in the Studio, in context
Modelled on an external reference lesson (ThriveNow Geometry) where the dynamic-geometry app is a shared in-site page the lesson hands a task to, rather than an inline embed. Our adaptation:
- **Studio v0.4** gains a **task lane** beside the canvas, a **"← Return to lesson"** button, and **save-my-construction / save-what-I-notice** (localStorage, auto-restored per task). It reads `?task=<id>&return=<relative-lesson-url>` from the URL; a single inline `TASKS` registry in the Studio is the one source of truth for task text and steps. Return paths are restricted to same-site relative URLs (no open-redirect).
- **Lessons launch in context:** each relevant Cornerstone (and the Final Task) carries an "Open in the Geometry Studio →" card linking to `../../tools/geometry-studio.html?task=…&return=…`. Same-tab navigation keeps the learner on the site; the Return button brings them straight back. Guided in-panel reveals stay, reframed as "see *why* it works"; the *doing* happens in the Studio with real instruments.
- **Build guard:** `build_site.validate_studio_links()` checks every lesson launch link on each build — task id exists in the Studio registry, Studio path resolves, return path resolves — and reports problems. This is the reusable pattern for every Constructions lesson (GC-01 is the first to use it: tasks `gc01-equilateral`, `gc01-sss`, `gc01-final`).

## Build note — GC-02 "Shapes that hold their form" (v0.1, built)
**Status:** live (`lessons/constructions/02-shapes-that-hold-their-form.html`); Learner + Supporting Adult guides generated; curriculum unit now 2 lessons; first lesson to use the Studio-launch pattern from the start.

**Hero (Gerry's choice):** *Rigidity detective*. Four hand-built SVG interactives sharing GC-01's construction helpers plus small linkage solvers (`circInt2`, `nearer`, 4-bar solve):
- **Connection — triangles in a truss:** a bridge frame with a "highlight the triangles" reveal; plants *why* structures use triangles.
- **Movement (hero) — rigidity detective:** a fixed-length **triangle** (apex = intersection of two fixed radii; dragging can only flip it, never deform it) beside a fixed-length **quadrilateral** as a 4-bar linkage (drag one corner on its circle, the opposite corner is solved) that flexes freely. The learner *feels* SSS rigidity vs a quadrilateral's one degree of freedom.
- **Reflection — why it holds:** both shapes with live angle readouts (quadrilateral draggable) + a reasoning MCQ tying rigidity to "no freedom left once three sides are fixed".
- **Creativity — brace it:** the flexing quadrilateral plus an "add a diagonal brace" that splits it into two shaded triangles and locks it (triangulation).

**Honest scope note:** rigidity is *demonstrated* in-panel (needs fixed-length bars, which the Studio's free points don't enforce); the Studio hand-off is for *constructing* the shapes and triangulating, with the rigidity insight carried by the panels and reasoning.

**Studio tasks added to the registry:** `gc02-triangle` (SSS build), `gc02-rhombus` (four equal sides), `gc02-final` (triangle + quadrilateral + diagonal). Launch cards in Movement, Creativity, Final Task; all validated by `validate_studio_links()`.

**Standards:** self-contained; Practice Companion + four-tier ladder; NEO Scratchpad (own key); palette [A,B,C,D,=,cm]; **QA gate 6a** confirmed (Q6 accepts 3/2 or 1.5); six Cornerstones in order; British English, "learner", no year labels. Guides engine gained `quad_diagram` (quadrilateral, optional diagonal → two triangles); worked examples: SSS triangle (6-5-7) and a braced quadrilateral.

## Build note — GC-03 "The perpendicular bisector" (v0.1, built)
**Status:** live (`lessons/constructions/03-the-perpendicular-bisector.html`); Learner + Supporting Adult guides generated; unit now 3 lessons.

**Hero (Gerry's choice):** *Equidistant locus tracer* — with the other three option-bank ideas woven across the panels (specific → general arc):
- **Connection — the fair meeting point:** two homes A, B; drag M until equidistant; discover more than one fair spot.
- **Movement (hero) — equidistant locus tracer:** drag P; live PA/PB readouts; P turns green and drops a mark when PA=PB; marks accumulate and *Reveal* draws the perpendicular bisector as their locus.
- **Reflection — guided construction:** step-gated equal arcs from A and B crossing at P and Q, join → bisector; explains *why* each crossing is equidistant (shared radius).
- **Creativity (stretch) — circle through two points:** slide centre O along the bisector; OA=OB always, so the circle catches both A and B.

**Studio tasks (registry):** `gc03-bisector` (two circles → Construct → Intersection(s) → Construct → Line), `gc03-circle` (Point on object on the bisector → circle through A catches B), `gc03-final` (bisector + midpoint via intersection, measure MA=MB). All use the explicit tool+menu, one-action-per-step, no-free-point instruction standard.

**Verification milestone (this session):** drove the real Studio through the equilateral, SSS and rhombus tasks in jsdom and drag-tested — the equilateral stayed equal (6.7→8.9 cm all three sides through a drag), proving Construct → Intersection yields genuinely dynamic constructions. Added a persistent "How constructions work here" tip to the Studio task panel (build dependent points with Construct → Point on object / Intersection, never the Point tool).

**Standards:** self-contained; Practice Companion + four-tier ladder; NEO Scratchpad (own key); palette [A,B,M,P,=,cm]; **QA gate 6a** (Q6 accepts 7/2 or 3.5); six Cornerstones in order; British English, "learner", no year labels. Guides engine gained `bisector_diagram` (equal arcs → crossings → bisector, optional midpoint M).

## Standing instruction standard (all Constructions lessons, GC-01 onward)
Studio task steps MUST: name the exact tool and the exact Construct-menu command; give one action per step; say which objects to select first; and explicitly warn that a point placed by eye with the Point tool is free and will collapse the construction on drag. Dependent points are always built with **Construct → Point on object** or **Construct → Intersection(s)**. This especially serves neurodivergent learners, who take instructions literally and deserve unambiguous, success-enabling steps.

## Build note — GC-04 "The angle bisector" (v0.1, built)
**Status:** live (`lessons/constructions/04-the-angle-bisector.html`); Learner + Supporting Adult guides generated; unit now 4 lessons.

**Hero (Gerry's choice):** *Equidistant-from-arms tracer* — the other three option-bank ideas woven across the panels, deliberately rhyming with GC-03:
- **Connection — the equal-distance path:** two roads meet at a junction V; drag P until equidistant from both arms; equal-distance spots line up.
- **Movement (hero) — equidistant-from-arms tracer:** drag P; live perpendicular-distance readouts to each arm; P turns green + drops a mark when equal; *Reveal* draws the angle bisector as their locus.
- **Reflection — guided construction + fold:** step-gated (arc from V cuts arms at P,Q → equal arcs → crossing R → join V–R), plus a **Fold** toggle showing the reflection symmetry that makes the halves equal.
- **Creativity (stretch) — circle touching both arms:** slide centre O along the bisector; equal perpendicular distances to both arms = radius, so the circle is tangent to both.

**Studio tasks (registry):** `gc04-bisector` (arc from V → Intersection(s) for P,Q → equal circles → Intersection(s) for R → join), `gc04-incircle` (bisector → Point on object → Perpendicular line + Intersection for the foot → circle touching both arms), `gc04-final` (bisector + Measure → Angle to confirm equal halves). All follow the explicit tool+menu, one-action-per-step, no-free-point standard.

**Standards:** self-contained; Practice Companion + four-tier ladder; NEO Scratchpad (own key); palette [A,B,V,=,°,cm]; **QA gate 6a** (Q6 accepts 45/2 or 22.5); six Cornerstones in order; British English, "learner", no year labels. Guides engine gained `anglebis_diagram` (angle, arc from V cutting arms, equal arcs → R, bisector V–R); worked examples: construct the bisector, and halving an 80° angle → 40°.

**Unit status:** Constructions & Loci now has GC-01…GC-04 live (compass/circle, rigidity, perpendicular bisector, angle bisector). Remaining per blueprint: GC-05 loci, GC-06 bearings, GC-07 compound plans.

## Build note — GC-05 "Loci" (v0.1, built)
**Status:** live (`lessons/constructions/05-loci.html`); Learner + Supporting Adult guides generated; unit now 5 lessons. Capstone of the bisector run — names and generalises the locus idea.

**Hero (Gerry's choice):** *Rule tracer* — the other three option-bank ideas woven across the panels:
- **Connection — the tethered goat:** drag the rope length; the circular grazing disc grows (a fixed distance from a point → circle/disc).
- **Movement (hero) — rule tracer:** pick "fixed distance from point O" or "fixed distance from segment AB", drag P, collect equal-distance marks; *Reveal* draws the locus — a circle, or a racetrack (parallel sides + semicircular ends).
- **Reflection — why the ends are round:** shows a point beside the segment (distance perpendicular → parallel side) vs beyond the end (nearest point is the endpoint → arc).
- **Creativity (stretch) — safe-zone region shader:** toggle two rules (within 3 of A; closer to A than B); grid-shaded overlap bounded by a circle and the perpendicular bisector.

**Studio tasks (registry):** `gc05-circle` (Circle + Point on object + Measure → locus is a circle), `gc05-region` (Construct → Perpendicular bisector + Circle → overlap region), `gc05-final` (compound locus: choose each boundary with the correct Construct command). Explicit tool+menu, no-free-point standard.

**Standards:** self-contained; Practice Companion + four-tier ladder; NEO Scratchpad (own key); palette [A,B,O,=,cm,r]; **QA gate 6a incl. the new number-word check** (Q3 accepts "four", Q6 accepts 7/2 and "three point five"); six Cornerstones in order; British English, "learner", no year labels. Guides engine gained `loci_diagram` (circle locus; region = circle ∩ perpendicular-bisector side). Worked examples: circle locus, and a two-rule region.

## Unit status — Constructions & Loci
Live: GC-01 compass/circle · GC-02 rigidity · GC-03 perpendicular bisector · GC-04 angle bisector · GC-05 loci. Remaining per blueprint: **GC-06 bearings**, **GC-07 compound plans in context**.
